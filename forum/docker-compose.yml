version: '3.8'

services:
  forum:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/root/data
    environment:
      - FORUM_DB_PATH=/root/data/forum.db
    restart: unless-stopped 